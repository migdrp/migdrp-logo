

  export interface Point {
    x: number;
    y: number;
  }
  
  export enum Fractals {
    Quartet = 'quartet',
    Gosper = 'gosper',
  }

  export enum LogoVertexState {
    Reduction = 'reduction',
    Expansion = 'expansion',
    Path = 'path',
  }

  


